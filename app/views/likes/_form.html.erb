<%= form_with(model: like, local: true) do |form| %>
  <% if like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
      <% like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Like_id %>
    <%= form.number_field :Like_id %>
  </div>

  <div class="field">
    <%= form.label :like %>
    <%= form.check_box :like %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
